package org.hse.example;

/**
 * Предоставляет метод, определяющий, является ли объект, реализующий интерфейс "счастливым"
 */
public interface Lucky {
    /**
     * @return true, если билет счастливый
     */
    boolean isLucky();
}
